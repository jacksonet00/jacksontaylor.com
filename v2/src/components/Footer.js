import { Box, Text } from '@chakra-ui/layout';

export const Footer = () => {
	return (
		<Box position="absolute" bottom={0} right={0} padding={6}>
			<Text fontSize="xl">🧘🏼‍♂️</Text>
		</Box>
	);
};
